
const getRecipes = async (req,res) => {

    //change recipes to get field TAGS

    //No Filtering needed
    if(!req.query){

    }

    //If query exists, filtering is needed
    const {kategorie,name,zutaten,erstelleraccount,tags} = req.query




}